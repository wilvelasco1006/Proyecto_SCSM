from django.apps import AppConfig


class ProyectoscsmappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'  # Tipo de campo para claves primarias
    name = 'ProyectoSCSMApp' # nombre de la app
